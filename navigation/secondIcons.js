
import Icon from "react-native-vector-icons/Feather";
import * as React from "react";
const icnProps = {
    color: "white",
    size: 20,
    focused: false
  };
export const settingIcon = icnProps =>(<Icon name="settings" color="white" size={20}/>); 
