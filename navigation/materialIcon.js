import Icon from "react-native-vector-icons/MaterialIcons";
import * as React from "react";
const icnProps = {
    color: "white",
    size: 20,
    focused: false
  };
export const exitIcon = icnProps =>(<Icon name="exit-to-app" color="white" size={20}/>); 